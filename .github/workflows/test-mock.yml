# 本運用の時はtestが実行されるworkflowを想定
name: test-mock

on:
  push:
    branches:
      - master
      - 'feature/*'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo test
      - name: Download artifact
        uses: dawidd6/action-download-artifact@v2
        with:
          workflow: test-mock2.yml
          workflow_conclusion: success 
          branch: master